global:
  scrape_interval: 5s

scrape_configs:
  # Node Exporter (host metrics)
  - job_name: node_exporter
    static_configs:
      - targets:
          - "{{ hostvars['web_app'][0].private_ip }}:9100"
        labels:
          instance: Directus
          role: app

      - targets:
          - "{{ hostvars['database'][0].private_ip }}:9100"
        labels:
          instance: Database
          role: database


  # cAdvisor (Docker metrics)
  - job_name: cadvisor
    static_configs:
      - targets:
          - "{{ hostvars['web_app'][0].private_ip }}:8080"
        labels:
          instance: Directus
          role: app

      - targets:
          - "{{ hostvars['database'][0].private_ip }}:8080"
        labels:
          instance: Database
          role: database

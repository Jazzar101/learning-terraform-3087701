---
services:
    nginx:
      image: nginx:latest
      container_name: nginx
      restart: always
      ports:
          - "80:80"
      volumes:
          - /home/ubuntu/nginx.conf:/etc/nginx/conf.d/default.conf:ro
